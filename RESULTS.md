# PoC 결과

`GitLab/GitHub → CI → Harbor → ArgoCD → Kubernetes` 파이프라인이 실제로 동작하는지,
폐쇄망에서도 성립하는지를 단계적으로 검증했다.

[PLAN.md](PLAN.md) 가 계획이고 이 문서가 결과다.
단계별 상세는 [docs/](docs/) 에 있고, 여기서는 **무엇이 증명됐고 무엇이 안 됐는지**를 정리한다.

| | |
|---|---|
| 기간 | Phase 0 ~ 7 |
| 이슈 · PR | 14 · 12 (전부 머지) |
| 스크립트 | 21개 |
| 문서 | 10개 + 이 문서 |
| 검증 항목 | 각 단계 완료 조건, 최종 40개(폐쇄망 20 · 격리 20) |

---

## 1. 증명된 것

**검증 목표 5개를 모두 달성했다.** 각 항목은 "된다" 가 아니라
**"안 되는 것이 실제로 막힌다"** 까지 확인했다.

| # | 목표 | 결과 | 근거 |
|---|---|---|---|
| 1 | 커밋 → 빌드 → 푸시 → 자동 배포 (무인) | 커밋 하나로 **약 4분** | [006](docs/006-phase4-ci-pipeline.md) |
| 2 | `selfHeal` — 수동 변경 자동 복구 | **약 4초** | [004](docs/004-phase2-argocd-gitops.md) |
| 3 | 취약점 스캔이 파이프라인을 실제로 차단 | Critical 검출 시 **pull 거부** | [005](docs/005-phase3-harbor-registry.md) |
| 4 | 인터넷 없이 반입 이미지만으로 배포 | 번들 **3.6GB** 로 파드 11개 기동 | [009](docs/009-phase6a-airgap-deployment.md) |
| 5 | 파트너사별 격리 | **4층**, 경계 침범 시도 전부 차단 | [010](docs/010-phase7-partner-isolation.md) |

### 구조적으로 확인된 것

**하나의 차트가 네 환경에서 값만 바꿔 돌았다.**
개발계 · 폐쇄망 · 파트너 A · 파트너 B. 차트를 고치지 않았다.
Phase 2에서 `charts/` 와 `envs/` 를 나눈 판단이 여기서 값을 했다.

```
charts/eshop/          구조와 기본값 (환경 무관)
envs/dev/              harbor.localtest.me/erp-hq
envs/airgap/           harbor.airgap/erp-hq        + .airgap 이름
envs/partner-a/        harbor.localtest.me/partner-a + 격리 켜기
envs/partner-b/        harbor.localtest.me/partner-b + 격리 켜기
```

**CI 로직을 전부 `scripts/` 에 뒀다.** 워크플로 YAML 은 얇은 래퍼다.
CI 도구를 GitHub Actions 에서 Jenkins 나 GitLab CI 로 바꾸는 비용이 작다 —
아직 열려 있는 질문에 대한 대비다.

### 실측 수치

| 구성 | 파드 | 메모리 |
|---|---|---|
| 개발계 (본사) | 11 | 약 6.0 GiB |
| 파트너 2곳 | 22 | 약 6.4 GiB |
| 폐쇄망 (별도 클러스터 + GitLab + 점프) | 11 | 8.4 GiB |

테넌트 한 벌이 **약 2.6 GiB** 다.

---

## 2. 기술 선택

**도구 선택은 다시 볼 필요가 없다고 본다.** 바꿔서 얻을 것이 보이지 않는다.

| 도구 | 판단 | 근거 |
|---|---|---|
| ArgoCD | 유지 | AppProject 의 테넌트 경계가 실제로 동작. Flux 가 가볍지만 UI 가 없어 감사·운영자 대응에 불리 |
| Helm | 유지 | 같은 차트가 네 환경에서 그대로 돌았다. 이 PoC 에서 가장 강한 증거 |
| Harbor | 유지 (조건부) | 취약점 게이트가 실제로 배포를 막는다. Nexus 는 이것을 못 한다 |
| CI 도구 | **미정** | GitHub Actions 는 대역이었다. 로직이 `scripts/` 에 있어 전환 비용은 작다 |
| SonarQube | 유지 | 특이사항 없음 |
| kind | 개발 전용 | 실제 환경으로 넘어가지 않는다 |

### Harbor 에 붙는 조건 — 미러링 대상이 둘이 된다

.NET 이라 **NuGet 미러가 별도로 필요하고 그건 Nexus 몫**이다.
폐쇄망에서는 동기화할 시스템이 둘이 된다.

Nexus 하나로 컨테이너와 NuGet 을 합칠 수 있지만, 그러면
"Critical 이면 pull 거부" 를 잃는다. Harbor + Nexus 둘 다 두고
그 운영 비용을 인정하는 쪽이 맞다고 본다.

### 도구보다 큰 판단 — MSA 의 인프라 세금

**파드 11개 중 6개가 앱, 5개가 인프라다** (Postgres × 2, Redis, RabbitMQ, SQL Server).
45% 가 앱이 아니고, 이것이 **테넌트마다 복제된다.**

15곳이면 인프라 파드만 75개다. 40 GiB 추산의 대부분이 여기서 나온다.
도구를 바꿔 줄일 수 있는 성질이 아니다.

**파트너마다 전체 스택을 복제할지, 공통 인프라를 공유할지가 이 설계의 가장 큰 갈림길이다.**
공유하는 순간 그 지점이 격리의 구멍이 된다. 도구 선택보다 훨씬 큰 결정이다.

---

## 3. 실제 환경으로 넘어가지 않는 것

이 PoC 는 랩톱 한 대의 kind 위에서 돌았다. 아래는 **다시 만들어야 하는 부분**이다.

### TLS — 가장 크다

지금 전 구간이 평문 HTTP 다. 켜는 순간 **쌓아온 모든 층을 건드린다.**

| 건드려야 하는 곳 | 지금 상태 |
|---|---|
| containerd `hosts.toml` | `http://` 엔드포인트 명시 |
| skopeo | `--tls-verify=false` |
| Harbor | `externalURL: http://...`, `expose.tls.enabled: false` |
| ArgoCD | `server.insecure: true` |
| pull secret | 평문 레지스트리 대상 |

폐쇄망이면 사설 CA 와 인증서 배포·갱신까지 필요하다. 작은 변경이 아니다.

### 그 밖

| 항목 | 지금 | 실제 |
|---|---|---|
| Ingress 진입 | `hostPort` (kind 에 LB 가 없어서) | LoadBalancer / MetalLB, 실제 DNS |
| 호스트 이름 | `*.localtest.me` (공개 DNS 가 127.0.0.1 로 해석) | 실제 도메인 |
| 스토리지 | local-path (단일 노드) | CSI 드라이버, 실제 스토리지 클래스 |
| 레지스트리 | Harbor 단일 레플리카, 내장 DB | HA 필요. 15곳 × 11파드 = 165개 동시 pull |
| 리소스 요청 | `50m/128Mi` — **추측값** | 실측 후 조정. limits 없으면 한 테넌트가 다른 테넌트를 굶긴다 |
| 시크릿 | Git 과 스크립트에 평문 | OpenBao / Sealed Secrets |

### DB 를 클러스터 안에 둘지 다시 봐야 한다

지금은 SQL Server 와 Postgres 가 파드로 뜬다.
ERP 에서 DB 는 보통 클러스터 밖 전용 서버다 — 백업, 라이선스, DBA 운영 때문이다.

**PoC 편의였고 실제로는 살아남지 않을 가능성이 높다.**
그러면 차트의 `infra` 절이 통째로 빠지고 접속 문자열만 남는다.
오히려 단순해지고, 테넌트당 메모리도 크게 줄어든다.

### 빌드·배포는 대체로 넘어간다

커밋 해시를 태그로 쓰는 방식, 실패 시 후속 단계 차단, 매니페스트 자동 갱신은
CI 도구가 바뀌어도 유지된다. 다만 두 가지 조건이 붙는다.

- **태그를 절대 재사용하면 안 된다.** Phase 4에서 캐시된 이미지가 게이트를 우회하는 것을 실제로 겪었다
- **폴링 60초는 15곳에서 부담이 된다.** GitLab 이 같은 망에 있으면 webhook 으로 바꿔야 한다

---

## 4. 환경 구성에서 실제로 부딪힌 문제

**이 절이 이 문서의 실질이다.** 아래는 전부 실제로 막혔던 것이고,
원인까지 확인해 각 단계 문서에 남겼다. 같은 자리에서 다시 막히지 않기 위한 목록이다.

### 생성 시점에만 적용되는 것

| 문제 | 원인 · 대응 |
|---|---|
| containerd 가 `certs.d` 를 읽지 않음 | `config_path` 는 **노드 생성 시점에만** 적용된다. `containerdConfigPatches` 로 넣고 클러스터를 다시 만들어야 한다 ([005](docs/005-phase3-harbor-registry.md)) |

### 조용히 무시되거나 다르게 동작하는 설정

| 문제 | 원인 · 대응 |
|---|---|
| CVE 허용목록을 등록해도 계속 차단 | Harbor `reuse_sys_cve_allowlist` 기본값이 `true`. 프로젝트 목록 대신 비어 있는 전역 목록을 본다 |
| Harbor 메타데이터가 일부만 반영 | API 가 키를 **하나씩만** 받는다. 여러 개를 보내면 400 |
| crane `--insecure` 로도 푸시 실패 | 레지스트리 연결에만 적용되고 **인증 흐름에는 적용되지 않는다**. skopeo `--dest-tls-verify=false` 로 교체 |
| Harbor 프로젝트 robot 이 조회되지 않음 | `/robots` 는 시스템 robot 만 준다. `q=Level=project,ProjectID=<id>` 가 필요하다. 없으면 빈 목록이 와서 "없다" 고 판단하고 재실행 시 CONFLICT ([010](docs/010-phase7-partner-isolation.md)) |
| ArgoCD prune 검증 실패 | 자원 추적은 라벨이 아니라 `argocd.argoproj.io/tracking-id` **어노테이션** ([004](docs/004-phase2-argocd-gitops.md)) |

### 한쪽만 되는 상태 — 원인 찾기가 가장 어려웠던 부류

| 문제 | 원인 · 대응 |
|---|---|
| Ingress 노드의 파드만 이미지를 받음 | Harbor 가 401 의 토큰 발급 주소를 `http://harbor.localtest.me/service/token` 으로 돌려주고, containerd 가 그것을 **다시 해석**해야 한다. `localtest.me` → 127.0.0.1 은 hostPort 를 가진 노드에서만 우연히 통했다. **모든 노드**의 `/etc/hosts` 에 항목 추가 |
| API 서버만 어드미션 웹훅에 못 닿음 | 폐쇄망에서 기본 경로를 지우자 ClusterIP 경로까지 사라졌다. iptables 가 파드 IP 로 바꾸기 **전에** 커널이 경로를 찾는다. 서비스 CIDR 경로만 따로 남긴다 ([009](docs/009-phase6a-airgap-deployment.md)) |
| 이미지는 다 들어왔는데 GitOps 만 안 돎 | 노드 `/etc/hosts` 는 containerd 가 이미지를 받을 때만 쓰인다. 파드 안 이름 해석은 **CoreDNS** 다. `hosts` 블록을 `kubernetes` 플러그인보다 앞에, `fallthrough` 와 함께 |

### 폐쇄망 특유

| 문제 | 원인 · 대응 |
|---|---|
| `--internal` 네트워크에서 노드가 부팅 실패 | 게이트웨이가 없는데 kind 진입 스크립트가 그 주소로 iptables 규칙을 만든다. 일반 네트워크 + 기본 경로 삭제로 전환 |
| 컨테이너가 자기 라우팅도 못 고침 | `NET_ADMIN` 필요. `ip route del` 이 조용히 실패하고 격리되지 않은 채 넘어간다 |
| `kind load` 가 blob 없음으로 실패 | `docker save` 가 멀티아키텍처 **인덱스를 통째로** 담는데 blob 은 받아온 하나뿐이다. `--platform` 고정 필요 |
| 플랫폼을 하나로 고정할 수 없음 | **Harbor 는 arm64 이미지를 내지 않는다.** ingress-nginx·GitLab 은 있다. 아카이브를 아키텍처별로 나눴다 |
| 다이제스트 고정 이미지를 못 받음 | `kind load` 한 이미지는 태그만 가진 기록으로 남아 `name@digest` 를 찾지 못한다. 검증 지점이 실행 시점에서 반입 시점(`MANIFEST.txt` 의 sha256)으로 옮겨간다 |
| GitLab 기동을 영원히 기다림 | `/-/readiness` 는 `monitoring_whitelist`(기본 `127.0.0.0/8`) 밖에서 **404** 를 준다 |

### 검증 자체가 틀렸던 것

**가장 위험한 부류다.** 통과했는데 아무것도 증명하지 못한 상태다.

| 문제 | 원인 · 대응 |
|---|---|
| "차단됨" 이 거짓 통과 | 애플리케이션 이미지(`dotnet/aspnet` 계열)에 `curl`·`wget`·`nc` 가 **없다.** 명령이 없어서 실패한 것을 네트워크가 막은 것으로 읽었다. **정책을 하나도 걸지 않아도 통과했을 검증이다.** 대조군(열려야 하는 경로)이 FAIL 로 뜬 덕분에 드러났다 |
| ArgoCD 거부를 확인 못 함 | 실제 문구는 `do not match any of the allowed destinations` 인데 다른 패턴으로 찾고 있었다. "자원이 안 생겼다" 는 약한 근거로 통과 중이었다 |
| 취약점 게이트를 통과해 버림 | **캐시된 이미지는 게이트를 거치지 않는다.** 태그가 같으면 노드에 있는 것을 그대로 쓴다. 커밋 해시를 태그로 쓰면서 드러났다 ([006](docs/006-phase4-ci-pipeline.md)) |
| 주문 검증이 갑자기 실패 | 목록 API 의 페이지 크기가 10이라 11번째 주문부터 1페이지에서 사라졌다. 이벤트 흐름은 정상인데 검증만 실패 |

**교훈: 차단을 확인하는 검증은 같은 방법으로 "열려야 하는 것" 도 함께 봐야 한다.**
"막혔다" 만 확인하면 도구가 고장 난 것과 구분되지 않는다.

### Helm · 차트

| 문제 | 원인 · 대응 |
|---|---|
| `.Values.apps.shopping-web` 접근 실패 | 하이픈이 점 표기를 깨뜨린다. `index .Values.apps "shopping-web"` |
| 값 파일 주석이 템플릿을 깨뜨림 | 주석 안의 `*/` 가 Go 템플릿 주석을 조기 종료 |
| 복제본만 바꿔도 전체 롤링 업데이트 | `checksum/config` 해시 대상에 `replicas` 가 들어 있었다. config·secrets 로 한정 |
| 파트너별로 서비스를 뺄 수 없음 | **Helm 은 맵에서 키를 지우지 못한다.** 값 파일만으로는 안 되고 차트에 `enabled` 플래그가 있어야 한다 |
| NetworkPolicy 적용 후 전부 멈춤 | egress 를 막으면 **DNS 부터 걸린다.** 증상이 "이름 해석 실패" 로 나타나 네트워크 정책 문제로 보이지 않는다 |

### 도구 · 운영

| 문제 | 원인 · 대응 |
|---|---|
| SonarQube 분석이 갑자기 실패 | 토큰 재발급이 **이전 토큰을 즉시 무효화**한다. 저장된 CI 시크릿과 함께 갱신해야 한다 |
| SonarQube 재설치 후 이력 소실 | 차트가 `helm uninstall` 시 **PVC 를 함께 지운다** |
| `docker exec` 파이프가 빈 입력을 읽음 | `-i` 없이는 stdin 이 전달되지 않는다. `no objects passed to apply` 로 나타났다. 두 번 겪었다 |
| SQL Server 가 arm64 에서 동작 | `binfmt_misc` 는 커널 전역이고 Rosetta 가 `F`(fix binary) 플래그로 등록돼 있어 kind 노드 안에서도 amd64 가 돈다. **기능은 되지만 성능 수치는 신뢰할 수 없다** ([003](docs/003-phase1-helm-charts.md)) |

---

## 5. 확인되지 않은 것

**경계를 분명히 해둔다.** 아래는 시작도 하지 않았다.

### 랩톱 한 대로는 만날 수 없는 것

| 항목 | 왜 중요한가 |
|---|---|
| **사내 프록시** | 인터넷망에 프록시가 있으면 `docker pull`·`dotnet restore`·`helm repo add` 가 전부 깨진다. 증상이 평범한 네트워크 오류로 보여 원인 찾기가 오래 걸린다. **국내 공공·대기업 환경에서 거의 항상 있다** |
| 노드 조인 · etcd · HA 제어 평면 | 단일 머신 kind 에는 없다 |
| 실제 LB · 방화벽 · 존 간 통신 | |
| CSI 스토리지 | 리사이즈, 스냅샷, 노드 어피니티 |
| 인증서 발급 · 갱신 | TLS 를 켜는 순간 필요 |
| NTP 어긋남 | 증상이 토큰·인증서 검증 실패로 나타난다 |
| 각 구성요소 업그레이드 | Kubernetes, Harbor, ArgoCD 모두 |

### 성능 · 용량

**이 PoC 로 사이징을 말하면 안 된다.**
랩톱 한 대이고, SQL Server 와 Harbor 는 Rosetta 에뮬레이션으로 돈다.

### 워크로드 특성

EShopMicroservices 는 쇼핑몰 샘플이다.
ERP 의 대량 트랜잭션, 배치, 장기 리포트, 레거시 연계는 성격이 다르다.
**파이프라인의 모양은 증명했지만 워크로드는 증명하지 않았다.**

### Day-2 운영

백업·복구, 업그레이드, 모니터링·알림, 로그 수집, 시크릿 관리, SSO.
지금 비밀번호는 `Harbor12345` 가 Git 에 그대로 있고 전 구간 평문 HTTP 다.
PoC 로는 정상이지만 **"운영 가능한가" 에는 답하지 못했다.**

---

## 6. 열려 있는 질문

| 질문 | 상태 | 영향 |
|---|---|---|
| **고객사 폐쇄망 범위** | 미정 | 이것이 정해져야 Phase 6b 범위가 나온다. 폐쇄망에서 **빌드까지** 하는지, 개발계에서 빌드해 반입하는지에 따라 NuGet·베이스 이미지 미러링 필요 여부가 갈린다 |
| **CI 도구** | 미정 | 아키텍처 문서는 Jenkins 를 전제하고, 9p 는 "파트너사 15곳에 Jenkins 까지 설치·운영시키는 게 맞나" 를 스스로 묻는다. GitLab CI 로 단일화하는 안과 비교 측정이 필요하다 |
| **파트너 인프라 공유 여부** | 미정 | 완전 복제면 15곳에 약 40 GiB. 공유하면 줄지만 공유 지점이 격리의 구멍이 된다 |
| **DB 를 클러스터 안에 둘지** | 미정 | 밖으로 빼면 차트가 단순해지고 메모리가 크게 준다 |

### 남겨둔 작업

| 항목 | 이슈 |
|---|---|
| Phase 6b — Trivy DB · 베이스 이미지 · NuGet 미러링 | 폐쇄망 범위 확정 후 |
| Phase 8 — 관측성, Keycloak SSO, OpenBao, Velero | |
| ResourceQuota · LimitRange | 파트너가 **만드는** 것은 막았으나 관리자가 **부여**하는 층이 없다 |
| 파트너 15곳 확장 | AppProject·Application 을 손으로 늘리고 있다. ApplicationSet 이 필요한 지점 |
| High 등급 취약 패키지 정리 | [#16](https://github.com/sunm2n/Kubernetes_devOps_Poc/issues/16) |
| 테스트 커버리지 측정 | [#19](https://github.com/sunm2n/Kubernetes_devOps_Poc/issues/19) |

---

## 7. 결론

**아키텍처가 성립하는지를 묻는 PoC 라면 답이 나왔다.** 구조적으로 안 되는 것은 발견되지 않았고,
검증 목표 5개가 모두 "막혀야 할 것이 막힌다" 수준까지 확인됐다.
도구 선택도 다시 볼 필요가 없다.

**어떻게 지을지와 운영에 얼마가 드는지를 묻는 것이라면 아직이다.**
실제 구축에서 시간을 먹을 곳은 세 군데로 보인다.

1. **TLS 전환** — 순수 작업량. 모든 층을 건드린다
2. **사내 프록시** — 순수 작업량. 증상이 모호해 초기에 시간을 많이 먹는다
3. **파트너 인프라를 복제할지 공유할지** — 결정. 되돌리기 비용이 크다

**사소한 문제는 "kind 위에서 만날 수 있는 것" 은 충분히 훑었고,
"실제 인프라에서만 만나는 것" 은 시작도 하지 않았다.** 이 경계가 현재 상태다.
