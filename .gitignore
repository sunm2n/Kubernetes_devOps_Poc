# ── 애플리케이션 소스 (별도 저장소) ──────────────────────────────
# EShopMicroservices는 sunm2n/net8_Microservices 로 독립 관리한다.
# 이 저장소는 인프라·GitOps·문서만 다룬다. (PLAN.md Phase 2 레포 분리 원칙)
EShopMicroservices-main/

# ── OS ───────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# ── 에디터 / IDE ─────────────────────────────────────────────────
.idea/
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
*.swp
*.swo
*~

# ── 시크릿 · 자격증명 (절대 커밋 금지) ───────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/*.key
*.p12
*.pfx
secrets.yaml
secrets.yml
*-secret.yaml
kubeconfig
*.kubeconfig
.kube/
sa-*.json

# ── Kubernetes / Helm ────────────────────────────────────────────
charts/**/charts/
charts/**/Chart.lock
*.tgz
rendered/
manifests-rendered/

# ── 폐쇄망 반출입 산출물 (용량 큼, Git 대상 아님) ────────────────
#
# scripts/61-export-bundle.sh 가 .bundle/ 에 수 GB 를 만든다.
# 이미지 tar 와 OCI blob 이라 Git 에 들어갈 것이 아니다.
# 무엇이 들어갔는지는 .bundle/MANIFEST.txt 가 남기고,
# 그 요약은 docs/009-phase6a-airgap-deployment.md 에 옮겨 적는다.
.bundle/
airgap/bundles/
airgap/**/*.tar
airgap/**/*.tar.gz
images/*.tar

# ── 빌드 산출물 ──────────────────────────────────────────────────
bin/
obj/
dist/
build/
node_modules/

# ── 로그 · 임시 파일 ─────────────────────────────────────────────
*.log
logs/
tmp/
.tmp/
scratch/
